os: linux
dist: focal
language: python

python:
  - 3.9

install:
  - sudo apt -y install python3-distutils
  - sudo apt -y install python3-pip
  - sudo apt install -y git
  - git clone https://github.com/kivy/buildozer.git
  - python3 -m site
  - ls -l
  - cd buildozer
  - ls -l
  - python3 setup.py install
  - ls -l
  - cd ..
  - ls -l

  - sudo apt update
  - travis_wait sudo apt install -y git zip unzip openjdk-8-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
  - travis_wait pip3 install --upgrade Cython==0.29.22 virtualenv
  - export PATH=$PATH:~/.local/bin/
  - ls -l
  - yes | buildozer android debug
  - ls -la
  - ls bin/ -l
  - echo "TODO"
  - travis_wait gem install dpl --pre
  - dpl releases --token $GITHUB_TOKEN --file "bin/test1-0.1-armeabi-v7a-debug.apk" --tag_name "v.0.1"
  - echo "TODO 2"